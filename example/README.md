# example

## Run the scoped accounts demo

```bash
flutter run -t lib/scoped_accounts/main.dart
```

This demo uses the new `WorkingRouterV2` scoped-first API and shows:
- global auth/add-account routes
- many account-scoped stacks
- preserved route state per account while switching scopes
